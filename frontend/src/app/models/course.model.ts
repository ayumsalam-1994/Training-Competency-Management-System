export interface Course {
    id?: number;
    title: string;
    description: string;
    videoUrl?: string; // For now, we'll just store the URL or file name
    // Add other fields as needed based on requirements
}
